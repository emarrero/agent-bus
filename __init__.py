"""Agent Bus — transparent multi-agent communication (text + audio).

Allows agents to communicate, delegate tasks and share messages
asynchronously, with multimodal support (text and audio).

Inspired by Google's A2A (Agent-to-Agent) protocol.
"""

__version__ = "0.3.0"
